# 医用报警系统设计规范（基于 YY 9706.108-2021）

> 版本: 2026-07-13-v1 | 适用: 输液瓶温控系统 Arduino Nano-ATmega328  
> 依据: 《医用电气设备 第1-8部分：基本安全和基本性能的通用要求 并列标准：医用电气设备和医用电气系统中报警系统的测试和指南》(YY 9706.108-2021)

---

## ◆ 设计依据与适配范围

### 一、本设备功能定位

| 项 | 描述 |
|---|---|
| 设备类型 | 医用电气设备（ME 设备）— 输液瓶加热器（非生命支持） |
| 使用场景 | 加热盐水/输液瓶至 37~41℃ 用于伤口冲洗 |
| 报警系统角色 | **风险控制措施**（见标准 §4，参考 GB 9706.1 12.3 条） |
| 操作者 | 临床医护人员（单操作者位） |
| 操作者位置 | 距设备正面 OLED 约 0.3~1.0m 处 |

### 二、依据标准的条款对照

| 标准 § | 要求 | 本设计状态 |
|---|---|---|
| §4 通用要求 | 报警系统作为风险控制措施并进行风险评估 | ✅ 见§三 |
| §5 ME 设备标识、标记和文件 | 控制器/指示灯/说明书标记 | ⏳ 见§七 |
| §6.1 报警状态 + 优先级分配 | 按 §6.1.2 表 1 分配 | ✅ 见§四 |
| §6.3.2 视觉报警信号 | 颜色/闪烁/符号 | ⚠️ 受限，见§五(1) |
| §6.3.3 听觉报警信号 | 脉冲群频率/间隔 | ⚠️ 受限，见§五(2) |
| §6.4 延迟说明 | 状态延迟 + 产生延迟 | ✅ 见§六 |
| §6.5 报警预置 | 预置参数 + 默认预置 | ✅ 见§八 |
| §6.6 报警限值 | 可调/不可调限值 | ✅ 见§九 |
| §6.7 报警系统安全性 | 防止未授权更改 | ✅ 见§十 |
| §6.8 报警信号非激活状况 | 关/暂停/已确认 + 提示信号 | ✅ 见§十一 |
| §6.10 栓锁/非栓锁信号 | 保持/自动停止 | ✅ 见§十二 |
| §6.12 报警系统日志 | 记录与标识 | ⏳ 见§十三 |

---

## ◆ 三、风险评估摘要（标准 §4）

### 1. 报警系统监护的危险源

| 危险源 | 触发条件 | 潜在伤害 | 伤害严重程度 |
|---|---|---|---|
| **过热输液** | 实测温度 > 42℃（人体组织损伤阈值） | 患者**烫伤**（不可逆损伤风险） | 中~重 |
| **传感器故障隐性加热** | NTC 短路/断路 + 加热仍开启 | 加热失控 → **烫伤** | 重 |
| **传感器故障无监护** | NTC 短路/断路 | 失去温度反馈 → 不知液温 | 中 |
| **过载超温** | 温度 > 50℃（硬件硬保护阈值） | 加热膜/线路**烧毁** | 财产损失 |
| **长时间加热运行** | 累计加热 ≥ 4 小时 | 干热/耗材老化 → 误烫伤 | 中 |
| **电源中断后状态丢失** | 失电 > 30s 后恢复 | 默认温度错误，可能过/欠温 | 轻 |

### 2. 由报警系统本身引入的次级风险

- 蜂鸣报警**可能惊扰**患者（输液场景患者意识清醒） → 需可由操作者**静音**（见§十一）
- 视觉报警闪烁**可能干扰**主显示读数 → 闪烁频率需节制（1Hz 低频）
- 误报警（假阳性）**导致忽略真实报警** → 通过 NTC 滤波（8 点移动平均）+ 故障滞回避免抖动

---

## ◆ 四、报警状态与优先级分配（对应标准 §6.1）

### 1. 报警状态分类

依据标准 §3.31（生理报警状态 physiological）与 §3.36（技术报警状态 technical）定义：

| 报警状态 ID | 类别 | 标准类别 | 描述 |
|---|---|---|---|
| **ALM_OVERHEAT_42** | 生理报警 | physiological | 任一通道实测温度 ≥ 42℃（人体组织损伤阈值） |
| **ALM_OVERHEAT_50** | 生理报警 | physiological（高严重度） | 实测温度 ≥ 50℃（硬件极限保护） |
| **ALM_NTC_FAULT** | 技术报警 | technical | 任一已启用的 NTC 短路或断路 |
| **ALM_BOTH_NTC_FAULT** | 技术报警 | technical（高严重度） | 所有启用通道 NTC 全部故障 → 等同失温监护 |
| **ALM_AUTO_OFF_4H** | 技术报警 | technical | 累计加热时长达 4 小时 → 强制关闭 |
| **ALM_POST_FAIL** | 技术报警 | technical | 上电自检失败（蜂鸣器/NTC/线路异常） |
| **ALM_TEMP_DEVIATION** | 生理报警（可选） | physiological | 实测温度偏离设定点 ≥ ±3℃ 持续 60s（**可选用作低优先级提示**） |

### 2. 优先级分配（依据标准 §6.1.2 表 1）

> 表 1 的判据：①**潜在伤害的严重程度** ②**响应失败的潜在结果**（即时/延迟/立即）

| ID | 潜在伤害严重 | 响应失败结果 | **分配优先级** | 理由 |
|---|---|---|---|---|
| ALM_OVERHEAT_50 | 不可逆损伤（烫伤）/设备损坏 | 即时死亡不可逆 | 🔴 **高优先级 (HIGH)** | 高温可能立刻烫伤患者 |
| ALM_BOTH_NTC_FAULT | 不可逆损伤（失温监护） | 延迟 → 不可逆 | 🔴 **高优先级 (HIGH)** | 完全失控 |
| ALM_POST_FAIL | 多种潜在 | 导致系统不可用 | 🔴 **高优先级 (HIGH)** | 阻止开机使用 |
| ALM_OVERHEAT_42 | 可逆损伤（轻微烫伤） | 立即 → 轻微损伤 | 🟡 **中优先级 (MEDIUM)** | 42℃ 接近痛阈但可逆 |
| ALM_NTC_FAULT（单路） | 可逆损伤（部分失监护） | 延迟 → 可逆 | 🟡 **中优先级 (MEDIUM)** | 仍有其余通道冗余 |
| ALM_AUTO_OFF_4H | 轻微损伤/不适 | 延迟 → 不适 | 🔵 **低优先级 (LOW)** | 仅提示需人工复位 |
| ALM_TEMP_DEVIATION | 不适 | 延迟 → 不适 | 🔵 **低优先级 (LOW)**（可选省略）| 操作者定期查看 OLED |

> ✅ **使用说明书需声明上述优先级分配及理由**（标准 §6.1.2 + §5.2.1）。

---

## ◆ 五、报警信号的产生（对应标准 §6.3）

### (1) 视觉报警信号（标准 §6.3.2）

#### 1.1 标准要求摘要（§6.3.2.2）

| 距离 | 要求 | 标准 § |
|---|---|---|
| **4m 处** | 须能识别报警存在 + 最高优先级；用**颜色 + 闪烁**指示 | 6.3.2.2.1 |
| **1m 处（操作者位）** | 须能识别**特定报警状态**；可用文本/符号 | 6.3.2.2.2 |

颜色规定（表 2）：

| 优先级 | 颜色 | 闪烁 |
|---|---|---|
| 高 | 红色 | 60/min（即 1Hz，亮暗比建议 50%） |
| 中 | 黄色 | 60/min（1Hz） |
| 低 | 蓝绿/黄色 | 常亮 |

符号：用 IEC 60417 符号 + 优先级附加 `!`、`!!`、`!!!`。

#### 1.2 本设备的硬件约束 & 适配方案

| 约束 | 现状 | 影响 |
|---|---|---|
| ❌ 无 RGB LED 指示灯 | 板上仅有 OLED + 蜂鸣器 | **无法满足 4m 处颜色识别要求** |
| ⚠️ OLED 为单色（白色像素） | 颜色编码不可用 | 用**符号数 + 闪烁频率 + 字号**区分 |
| ✅ OLED 在 1m 内可清晰阅读 | 操作者距离 ≤ 1m | 满足 1m 处识别要求 |

#### 1.3 设计方案（折中**

```text
[OLED 128×64 报警页布局示例]

高优先级（ HIGH ）

   *** ALARM ***          ← 标题, 闪烁频率 1Hz (500ms 亮/500ms 暗)
   !!! HIGH !!!           ← 优先级符号, 闪烁
   CH1: 50.2 C            ← 触发状态详情
   SW3=MUTE 3s           ← 操作提示
```

| 优先级 | OLED 文本符号 | 闪烁频率 | 字号 |
|---|---|---|---|
| 高 HIGH | `!!! HIGH !!!` | **1Hz**（亮 500ms / 暗 500ms 整屏闪） | 2×（大字体 16px+） |
| 中 MEDIUM | `!! MED !!` | **0.5Hz**（亮 1s / 暗 1s） | 1.5×（中字体） |
| 低 LOW | `! LOW`（常亮不闪） | 0Hz（不闪烁） | 1×（标准字） |

**附加视觉指示：**
- 主页**状态栏**常驻一个 `!` 图标栏（高 = `!!!` 闪烁，中 = `!!`，低 = `!`）。
- 触发通道名以**反白（黑底白字↔白底黑字）**闪烁。

> 📝 **使用说明书须明确说明**：本设备未提供 4m 处可识别的颜色指示灯，提醒 операторы 必须保持 ≤1m 距离定期查看 OLED 显示。

#### 1.4 多报警同时存在的处理（标准 §6.3.2.2.2 倒数第三段）

- 默认行为：**显示最高优先级报警**，其余在状态栏以小图标列出。
- 不实现 6.2 节"智能报警系统"，因为本设备报警数量少（≤7 种），不必要。

---

### (2) 听觉报警信号（标准 §6.3.3）

#### 2.1 标准要求（表 3 脉冲群特征 + 表 4 脉冲特征）

| 参数 | 高优先级 | 中优先级 | 低优先级 |
|---|---|---|---|
| 脉冲数/脉冲群 | 5 | 3 | 1~2 |
| 脉冲群间期 | 2.5~15s | 2.5~30s | 不重复 |
| 脉冲频率 f₀ | 150~1000 Hz |
| 谐波分量数 | ≥4（300~4000Hz） |
| 脉冲有效持续 t₂a | 75~200ms | 125~250ms |

**脉冲间隔（表 3）：** 高优 5 脉冲：x-x-y-x-x-y-x-x-y-x-x（x=50~125ms, y=125~250ms）。
**脉冲群至少完整发出一次**（高优），中/低优先级至少完成一个脉冲群。

#### 2.2 本设备硬件约束 & 替代方案

| 约束 | 现状 | 影响 |
|---|---|---|
| ⚠️ 蜂鸣器为**有源**（PB-1224PE-05Q, 固定 2.4kHz, 85dB） | 单引脚 D5 数字电平控制 ON/OFF | **f₀ 不可改，谐波数不可控** |
| ✅ 声压级足够（85dB@10cm） | 高于一般病房环境噪声 | 满足可听度 |
| ❌ 无法生成多频率脉冲 | 不能区分 f₀ 编码 | **仅能用脉冲数 + 脉冲群重复率区分优先级** |

#### 2.3 设计方案（按表 3 的"脉冲数"维度编码优先级）

> 因硬件限定为固定 2.4kHz 方波，无法满足标准表 4 的脉冲频率/谐波数要求。**采用降级方案**：仅依据**脉冲数 + 脉冲群间期**区分优先级。**使用说明书须如实声明这一限制。**

| 优先级 | 单脉冲宽度 t₂a | 脉冲数 | 脉冲间隔 x | 脉冲群重复周期 T₂ |
|---|---|---|---|---|
| 🟢 HIGH | 125 ms | **5** | 100 ms | **3.0 s** |
| 🟡 MEDIUM | 150 ms | **3** | 130 ms | **6.0 s** |
| 🔵 LOW | 200 ms | **1** | — | 仅提示一次后不再重复（操作者须主动查看） |

**脉冲群时序（节拍状态机实现）：**
```
HIGH  (3s 循环):  [哔 哔 哔 哔 哔] ──静 2.4s── [哔 哔 哔 哔 哔] ...
                  |125|100|125|100|125|100|125|100|125| ── 总 ≈ 1s
MEDIUM(6s 循环):  [哔 哔 哔] ───静 5.6s─── [哔 哔 哔] ...
                  |150|130|150|130|150| ── 总 ≈ 0.7s
LOW  (单次):      [哔] ── (结束, 60s 后若仍未解除再发一次)
```

#### 2.4 音量调节（标准 §6.3.3.2 + §6.3.3.3）

- 本设备蜂鸣器直驱 5V 数字电平，**不可调节音量**（无 PWM DAC 通道分配给蜂鸣器）。
- 标准要求：高/中优先级声压级**范围须在使用说明书中说明** → 本设备声明：**≈ 85 dB(A) @ 10cm（额定）**。
- 标准要求操作者可调音量的最小值/说明 → **本设备不支持音量调节，作为限制写入说明书**。
- 标准要求：**不能让操作者将音量调到 0** → 本设备天然满足（无音量控制）。

---

### (3) 语音/振动等其他报警

> 不适用（本设备无语音合成器/振动马达）。

---

## ◆ 六、报警延迟说明（标准 §6.4）

### 1. 报警状态延迟（trigger → 系统识别为报警状态）

| 路径 | 延迟源 | 估计值 |
|---|---|---|
| NTC 采样 → 滤波器响应（8 点移动平均） | 4 × 500ms（PID 周期）= 2s 满量程响应 | ≤ 2.0 s |
| NTC 故障判定（滞回防抖） | 状态连续保持 ≥ 1 个采样周期 | ≤ 0.5 s |
| 超温报警触发（≥50℃） | 在 PID 计算同周期内立即触发 | ≤ 0.5 s |
| **总计最大报警状态延迟** | — | **≤ 3.0 s** |

### 2. 报警信号产生延迟（识别报警 → 产生视觉/听觉信号）

| 路径 | 估计值 |
|---|---|
| 状态置位 → UI render 调用 | ≤ 500ms（UI 刷新周期） |
| 状态置位 → 蜂鸣节拍下一拍 | ≤ 3s（高优，节拍周期） |
| **最大报警信号产生延迟** | **≤ 3.0 s**（听觉） / **≤ 0.5 s**（视觉） |

### 3. 标准判定

> 标准要求：最大报警状态延迟 + 最大报警信号产生延迟 **之和** > 10s，须在使用说明书声明；
> 平均值之和 > 5s 也须声明。

- 本设备最大和：3.0s + 3.0s = **6.0s**（< 10s）→ ✅ **无需声明最大值**
- 本设备平均和：约 1.5s + 1.5s = **3.0s**（< 5s）→ ✅ **无需声明平均值**

✅ **本设备报警延迟满足标准上限，使用说明书无需特别声明延迟信息。**

### 4. 分布式报警系统延迟（标准 §6.4.2）

> **不适用**（本设备为独立运行，无远程报警发送/接收接口）。

---

## ◆ 七、ME 设备标识、标记和文件（标准 §5）

### 1. 控制器和指示灯（§5.1）

| 标识对象 | 方式 | 备注 |
|---|---|---|
| SW1 (D2) | 字标 `△ / ▽` 或文字 `▲ TEMP` | 温度调高 |
| SW2 (D3) | 字标 `▽ / ▽` 或文字 `▼ TEMP` | 温度调低 |
| SW3 (D4) | 字标 ON/OFF 或 IEC 60417-5007/5008 | 温控开关 + 报警静音 |
| OLED | 文字"ALARM"显示报警存在 | 视觉报警信号 |
| 蜂鸣器发声口 | 无须标识 | 仅提示音 |

### 2. 使用说明书（§5.2.1）必须包含

- [ ] 报警系统概述：所有可能报警状态的**列表 + 描述**（对应 §四的表）
- [ ] 每种报警状态的优先级（§四.2 表）
- [ ] 报警状态如何确定的总结（NTC 比较 + 滞回逻辑）
- [ ] 操作者位置：**正面 0.3~1.0m 处**
- [ ] **测试报警系统的方法**：上电时蜂鸣器自检鸣响 200ms（见 §十四 POST）
- [ ] **限制声明**：
  - 无 4m 视觉报警指示灯（颜色编码）
  - 蜂鸣器为固定 2.4kHz 有源蜂鸣器，仅按脉冲数编码优先级
  - 蜂鸣音量不可调
- [ ] 警告：设置报警限值超过极限会导致报警系统失效 → 不适用（用户不可设限值，见§九）

### 3. 技术说明书（§5.2.2）必须包含

- [ ] 禁止访问/储改报警设置的访问控制机制（见§十）
- [ ] 报警系统日志的容量与溢出策略（见§十三，如启用）

---

## ◆ 八、报警预置（标准 §6.5）

### 1. 配置参数定义

> 标准定义"报警预置（alarm preset）"为**会影响报警系统性能的配置参数集合**。本设备的报警预置**等价于温度设定值**（因为温度阈值由设定值推导）。

本设备仅支持**1 个报警预置**（一组温度设定值），无多套预置存储。

### 2. 制造商设定的报警预置（§6.5.2）

| 预置项 | 制造商默认值 |
|---|---|
| 默认目标温度 | **38.0℃** |
| 调温范围 | **37.0 ~ 41.0℃** |
| 步进 | **0.5℃** |
| 超温警告阈值（中优先级） | **42.0℃**（不可调） |
| 超温硬切断阈值（高优先级） | **50.0℃**（不可调） |
| 超温解锁（迟滞） | **48.0℃** 以下（不可调） |
| 自动关机累计加热时长 | **4 小时**（不可调） |

> ✅ **本设备至少提供 1 个制造商预置**（默认值），满足标准 §6.5.2 要求。

### 3. 责任方/操作者设定的预置（§6.5.3.1 — 单预置系统）

标准要求（含 1 个预置的系统）：

| 标准 § | 要求 | 本设备实现 |
|---|---|---|
| 6.5.3.1 a | **防止操作者存储对预置的修改**，仅限责任方 | ✅ 当前操作者**无法永久保存**（断电即丢失，每次上电回 38℃）|
| 6.5.3.1 b | **提供责任方恢复到制造商预置的方法** | ✅ 上电自动恢复 TEMP_DEFAULT，等于"恢复默认" |

> 由于本设备**无 EEPROM 持久化**（Nano ATmega328P 的 EEPROM 可选启用，当前未启用），所有运行期修改**掉电即丢失**，等同于"预置不可被操作者永久改变"。如果未来启用 EEPROM，**必须**实现"写入 EEPROM 仅限责任方"的访问控制（见§十）。

### 4. 默认报警预置（§6.5.4）

#### 4.1 默认预置选择触发（标准 §6.5.4.2）

| 触发条件 | 标准条 | 本设备动作 |
|---|---|---|
| a) 制造商规定时间间隔后无一-操作者交互 | 6.5.4.2 a | ⏳ **可选**：10 分钟无操作自动回 38℃（待定） |
| b) 启动报警系统 | 6.5.4.2 b | ✅ **上电即默认 38℃** |
| c) 导入新患者功能 | 6.5.4.2 c | ❌ 不适用（本设备无患者标识） |
| d) 完全失电超过自动恢复报警设置时间后恢复供电 | 6.5.4.2 d | ✅ **任何失电后上电即回 38℃** |

#### 4.2 电源中断 < 30s 的行为（§6.5.5）

> 标准要求：失电**不超过 30s**时，失电前的报警设置应自动恢复。

| 本设备现状 | 符合性 |
|---|---|
| 状态变量保存在 RAM 中，**20~50ms 失电即丢失** | ❌ **不满足** |
| 短时失电恢复后，温度设定回默认 38℃ | 不符合 §6.5.5 |

**待办**: 若需符合 §6.5.5，需启用 ATmega328P EEPROM（1KB），在温度/状态变化时写入（注意 wear leveling），上电时若 EEPROM 标志位有效则恢复；否则用默认值。
**优先级**: 🟡 中等（输液瓶加热器为短时使用设备，断电影响可控）

---

## ◆ 九、报警限值（标准 §6.6）

### 1. 限值清单

| 限值名称 | 类型 | 值 | 标识方式 |
|---|---|---|---|
| 用户目标温度 | **操作者可调** | 37~41℃ | OLED 主页 `Set: 38.0C` 持续显示 |
| 超温警告阈值 | **不可调** | 42℃ | 仅说明书标识 |
| 超温硬切断阈值 | **不可调** | 50℃ | 仅说明书标识（硬件保护） |
| NTC 故障阈值 ADC | **不可调** | <20 短路 / >1000 断路 | 仅技术说明书标识 |

### 2. 调整时的报警系统运行（§6.6.2.3）

> 标准要求：调节限值/预置期间，报警系统**持续正常工作**。

✅ 当前实现：温度调节通过按键中断/事件方式处理，报警扫描逻辑与 PID 并行运行，**报警判定不因调温而暂停**。

### 3. 自动设置限值的指示（§6.6.2.2）

- **不适用**：本设备无自动温控设定点函数（如根据生理变量自动推导目标温度）。

### 4. 限值不可超过极限的警告（§5.2.1 使用说明书）

✅ 已通过硬件/固件约束实现：
- 温度调高上限被 `TEMP_MAX = 41℃` 硬夹住（远低于超温切断 50℃）。
- 操作者无法通过任何方式让报警限值"超过极限导致报警失效"。

---

## ◆ 十、报警系统安全性（标准 §6.7）

### 1. 需要访问控制的功能

| 标准 § | 功能 | 是否需要责任方控制 |
|---|---|---|
| 6.6 报警限值 | 温度设定值 | ❌ 操作者可调（37~41℃ 硬限内），**不须控制** |
| 6.8.1 静音/暂停 | 听觉报警静音（≤120s，自动恢复） | ✅ 操作者可用 SW3 + 长按（见§十一） |
| 6.8.3 整体报警关闭 | 系统级报警关闭 | ❌ **本设备不提供**（出于安全考虑禁止整体关闭） |
| 6.8.5 时间间隔上限 | 静音时长上限 | ✅ 默认 120s（**不可调**，等同责任方固化） |
| 6.10 报警栓锁设置 | 栓锁/非栓锁选择 | ✅ **固化为非栓锁**（见§十二），**不可由操作者更改** |
| 加热使能 | 是否允许加热 | ✅ SW3 操作者可切（关加热只是临时停止，不改变报警系统） |

### 2. 访问控制实现（满足 §6.7）

| 控制层级 | 方式 | 本设备实现 |
|---|---|---|
| 责任方级 | 密码 / 工具 / 物理 | ⏳ **未来**：可通过长按 SW1+SW3 同时 ≥ 10s 进入"责任方模式"修改默认参数（未实现）|
| 操作者级 | 普通使用 | 当前操作者可调：温度 ±0.5℃、开关温控、临时静音报警 |

**当前固件中已固化为不可变的项**（自动满足"责任方独占"）：
- 默认温度 38℃（不可调）
- 超温阈值 42℃/50℃（不可调）
- 自动关机时长 4h（不可调）
- 静音最长 120s（不可调）
- 报警延迟（滤波参数，不可调）

### 3. 使用说明书要求（§6.7）

> 技术说明书须说明"禁止访问或储改"的措施。

📝 **写入说明书**：所有报警系统关键参数（超温阈值、自动关机时长、默认温度、静音时长）均为制造商固化值，操作者无法通过任何按键组合改变。

---

## ◆ 十一、报警信号非激活状况（标准 §6.8）

### 1. 非激活状况类型（标准 §3.4~3.5、§6.8.1）

| 非激活状况 | 标准定义 | 本设计是否提供 |
|---|---|---|
| **报警关闭** (alarm off, 无限期无报警) | §3.4 | ❌ **不提供**（出于安全考虑禁止整体关闭） |
| **报警暂停** (alarm paused, 有限期无报警) | §3.5 | ❌ 不提供整体暂停 |
| **声音关闭** (audio off, 无限期无声觉) | §3.12 | ❌ **不提供**（禁止无声运行） |
| **声音暂停** (audio paused, 有限期无声) | §3.13 | ✅ **提供**（操作者可临时静音） |
| **已确认** (acknowledged) | §3.37 | ✅ **提供**（操作者动作暂停声觉报警直到状态消失） |

### 2. 本设计的"声音暂停/已确认"机制（标准 §6.8 + §3.37）

> 根据标准 §3.37 注 1：**已确认**仅影响操作者动作时已激活的报警信号，不影响后续新激活的报警。

**操作者触发方式**：**长按 SW3 ≥ 2 秒**

| 项 | 规则 |
|---|---|
| 触发条件 | 当前存在高/中优先级报警，且操作者长按 SW3 |
| 静音时长 | **最长 120 秒**（2 分钟，可提前结束） |
| 静音期间行为 | **听觉报警信号停止**；**视觉报警信号（OLED）仍然闪烁/显示** |
| 静音到期后 | 自动恢复听觉报警 |
| 静音期间新报警产生 | **不受静音影响**（例如静音过程中又触发更高级别报警 → 立即重新鸣响） |
| 静音期间已确认报警状态消失 | 立即解锁，准备响应后续报警 |

> ⚠️ 标准要求：**已确认不应使 1m 处视觉报警信号处于非激活**（§6.8.1 倒数第三段）→ 本次设计中 OLED 视觉报警在静音期间**持续显示**，满足要求。
> ⚠️ 标准要求：已确认可造成报警状态优先级降级，包括把低优先级降级为信息信号 → **本设备不实现降级**（保持原优先级）。

### 3. 提示信号（Reminder Signal，标准 §3.34 + §6.8.2）

> **提示信号**：报警系统在报警信号非激活状况下，提醒操作者仍存在激活报警状态的信号。

| 项 | 本设计 |
|---|---|
| 是否提供 | ✅ 提供最低限度提示 |
| 提示信号特征 | OLED 上显示静音倒计时（`MUTE 90s` 倒计时）+ 状态栏闪烁的 `!` 图标 |
| 两提示信号间隔 | 每 30 秒蜂鸣器"哔"一声（80ms 短鸣，单音） |
| 责任方访问控制 | 不需要（提示信号开关点定为永久开启，无须保护） |

### 4. 整体报警关闭/声音关闭（标准 §6.8.3）

> 标准允许提供整体报警关闭/声音关闭，需经责任方设置（§6.7 防止临床操作者改变）。

**本设备**：**不提供**整体报警关闭/声音关闭功能。理由：
1. 本设备用途为患者输液加热，存在烫伤风险，**不应允许操作者持续屏蔽报警**。
2. 减少合规复杂度（无需责任方访问控制层）。

### 5. 非激活状况终止（标准 §6.8.4）

| 触发 | 行为 |
|---|---|
| 静音倒计时结束 | 自动终止"声音暂停"，恢复声觉报警 |
| 受影响的报警状态消失（如 NTC 恢复、温度回落） | 锁存标志归位，报警视觉/听觉停止；若期间无新报警则恢复常态 |
| 操作者再次长按 SW3 | 立即终止静音（提前恢复报警） |

### 6. 标记（标准 §6.8.5）

| 状况 | 强制视觉指示 | 标准符号 |
|---|---|---|
| 声音暂停 / 定时已确认 | OLED 显示 `MUTE` + 倒计时 | IEC 60417 No.5511（"喇叭带斜线"）可选 |
| 报警暂停（不适用） | — | — |
| 报警关闭 / 声音关闭（不适用） | — | — |

> 由于本设备硬件无独立 LED，所有视觉指示通过 OLED 文字实现。"静音时长最大值"写死为 120s，**不可由操作者调整**，等同责任方固化。

---

## ◆ 十二、栓锁 vs 非栓锁报警信号（标准 §6.10）

| 类型 | 定义 | 本设备分配 |
|---|---|---|
| **非栓锁** (non-latching) | 触发事件消失 → 信号自动停止 | ALM_OVERHEAT_42（温度回落 → 自动消除） |
| **栓锁** (latching) | 触发事件消失 → 信号持续，需操作者动作停止 | ALM_OVERHEAT_50、ALM_BOTH_NTC_FAULT、ALM_AUTO_OFF_4H、ALM_POST_FAIL |

### 1. 栓锁策略分配

| 报警 ID | 是否栓锁 | 复位方式 | 理由 |
|---|---|---|---|
| ALM_OVERHEAT_50 | ✅ **栓锁** | SW3 短按 + 温度 < 48℃ | 严重隐患须操作者确认 |
| ALM_BOTH_NTC_FAULT | ✅ **栓锁** | SW3 短按 + NTC 恢复正常 | 完全失监护状态须确认 |
| ALM_AUTO_OFF_4H | ✅ **栓锁** | SW3 短按（清零计时 + 重新启用）| 须人工重置 |
| ALM_POST_FAIL | ✅ **栓锁**（系统进入死循环）| 必须断电检修 | 故障未排除前不应继续使用 |
| ALM_OVERHEAT_42 | ❌ 非栓锁 | 自动：温度 < 41.5℃ 时消除 | 短期可逆 |
| ALM_NTC_FAULT（单路） | ❌ 非栓锁 | 自动：NTC 恢复后消除 | 仍有冗余通道 |
| ALM_TEMP_DEVIATION | ❌ 非栓锁 | 自动：误差 < ±2℃ 消除 | 提示类 |

### 2. 优先级最低脉冲完成（标准 §6.10 a）

> 标准要求：对于持续很短的报警状态，**中优先级听觉报警信号应至少完成一个完整脉冲群，高优先级听觉报警信号应至少完成半个脉冲群**，除非操作者使其非激活。

**实现方式**：状态机一旦进入 HIGH/MEDIUM 报警态，蜂鸣节拍状态机在结束一个完整脉冲群前**不接受"状态解除"信号**。即触发态立即开始节拍后，即使温度瞬间回落，**也会完成至少 5/3 个脉冲**。

### 3. 选择限制（标准 §6.10 末段）

> 标准要求：栓锁 vs 非栓锁的选择**仅限责任方**。

✅ **本设计**：栓锁/非栓锁分配**在固件中固化**，操作者无法选择/更改，等同责任方独占。

---

## ◆ 十三、报警系统日志（标准 §6.12，可选）

### 1. 是否启用

| 标准条 | 要求 | 本设备 |
|---|---|---|
| §6.12 a | 记录**高优先级报警状态**的发生时间 | ⏳ 受限 |
| §6.12 b | 说明书须说明记录的时基（实时时间/相对时间）| — |
| §6.12 c | 说明书须说明失电后日志内容如何变化 | — |
| §6.12 d | 说明书须说明日志满后的处理 | — |

**约束**：ATmega328P 仅有 **2KB RAM + 1KB EEPROM + 32KB Flash**。完整日志（日期时间）需 RTC，本设备**未配备 RTC**。

### 2. 推荐实现方案（可选）

**轻量级 RAM 循环日志**：

```cpp
struct AlarmLogEntry {
    uint8_t  alarmId;        // ALM_OVERHEAT_50 等
    uint32_t bootMs;         // 上电以来的毫秒数 (相对时间)
    uint8_t  channel;        // 0~3 或 0xFF=系统级
};

#define ALARM_LOG_CAP   16                  // 仅保留最近 16 条
AlarmLogEntry logBuf[ALARM_LOG_CAP];
uint8_t       logHead = 0;                  // 循环索引 (满后弃旧)
```

- 满后策略：**循环覆盖最早条目**（FIFO 弃旧，对应 §6.12 d "示例 2"）
- 时间基准：**开机后毫秒数（millis()）**，非实时时钟 → 说明书须声明
- 失电后内容变化：**全部丢失（RAM）** → 说明书须声明
- 何时记录：**每次高优 + 中优报警的"产生"和"消除"事件**（共两条）
- 操作接口：**无 UI**（防止操作者编辑/重置，符合 §6.12 f）
- 串口调试口可输出（仅供制造商服务访问）

> 若不实现日志，需在使用说明书明确说明"本设备不具备报警系统日志"。

---

## ◆ 十四、上电自检 POST（报警系统可用性验证，对应标准 §5.2.1）

> 标准要求说明书须说明**怎样及何时去检验报警系统是否在起作用**。

### 1. POST 流程（开机顺序执行）

| 步骤 | 检查项 | 失败动作 |
|---|---|---|
| 1 | 蜂鸣器鸣响 200ms 测试发声能力 | 进入 ALM_POST_FAIL 死循环 + OLED "BUZZER ERR" |
| 2 | NTC 4 连采 + 短路判定 (ADC<20) | 进入 ALM_POST_FAIL 死循环 + OLED "NTC SHORT CHx" |
| 3 | NTC 4 连采 + 断路判定 (ADC>1000) | 进入 ALM_POST_FAIL 死循环 + OLED "NTC OPEN CHx" |
| 4 | 所有检查通过 → 蜂鸣器短鸣一声 200ms 示意正常 | 进入主循环 |

### 2. POST 视觉/听觉信号特征

| 阶段 | 视觉 | 听觉 |
|---|---|---|
| POST 中 | OLED 显示 `SELF TEST...` | 无 |
| POST 失败 | OLED 全屏闪烁（1Hz）显示 `!!! ALARM !!!` + 错误码 | 蜂鸣器**长鸣**持续 |
| POST 通过 | OLED 切换到主页 | 200ms 短哔一声 |

> ✅ POST 即为标准 §5.2.1 要求的"何时检验报警系统是否起作用"的回答。说明书须建议操作者留意上电时的提示音。

---

## ◆ 十五、固件实施汇总（接口与数据结构）

### 1. 推荐新增模块（独立于现有 PID/NTC）

| 模块文件 | 职责 |
|---|---|
| `src/alarm_manager.h` / `.cpp` | 报警状态机、优先级计算、静音/已确认管理、视觉/听觉节拍生成 |
| `src/config.h` 增补 | 报警阈值宏、节拍时间宏 |

### 2. 核心数据结构建议

```cpp
// 报警优先级
enum class AlarmPriority : uint8_t {
    NONE    = 0,
    LOW     = 1,
    MEDIUM  = 2,
    HIGH    = 3,
};

// 单个报警状态描述
struct AlarmState {
    uint8_t         id;             // ALM_OVERHEAT_50 等
    AlarmPriority   priority;
    bool            active;         // 当前是否进入此报警态
    bool            latched;        // 是否栓锁
    bool            visualPts;      // 视觉信号是否激活
    bool            audioAcked;     // 是否被操作者 ACK
    uint32_t        triggerBootMs;  // 进入时刻 (millis)
    uint32_t        ackDeadlineMs;  // 静音到期时刻
};

// 报警管理器
class AlarmManager {
public:
    void begin();
    void update(uint32_t nowMs);                // 周期调用 (例如 PID 周期 500ms)
    void trigger(uint8_t id, uint8_t channel);  // 触发某报警
    void clear(uint8_t id);                     // 自动清除非栓锁报警
    void operatorAck();                          // 操作者长按 SW3 触发
    void reset(uint8_t id);                      // 操作者短按复位栓锁报警

    AlarmPriority currentPriority() const;      // 当前活跃的最高优先级
    bool          isVisualOn() const;           // 用于驱动 OLED 闪烁
    bool          isAudioOn(uint32_t nowMs);    // 用于驱动蜂鸣器 (含脉冲节拍)
    AlarmState*   topAlarm();                    // 取当前最高优先级报警 (UI 显示)
};
```

### 3. 节拍状态机（蜂鸣器驱动）

```cpp
// 蜂鸣器状态机 (50ms tick 调用一次)
enum class BeepPhase : uint8_t { IDLE, PULSING, INTER_BURST };
struct BeepSequencer {
    BeepPhase phase;
    uint8_t   pulseIdx;        // 已完成脉冲数
    uint8_t   pulseTotal;      // HIGH=5, MED=3, LOW=1
    uint16_t  pulseOnMs;       // HIGH=125, MED=150, LOW=200
    uint16_t  pulseIntervalMs; // HIGH=100, MED=130
    uint16_t  burstIntervalMs; // HIGH=2400, MED=5600
    uint32_t  nextTickMs;
};
```

### 4. main.cpp 集成点

| 位置 | 修改 |
|---|---|
| `setup()` 末尾 | 调用 `alarmManager.begin()` 与 POST 流程 |
| `loop()` 内 PID 计算后 | 调用 `alarmManager.update(millis())` |
| 超温/故障判断点 | 调用 `alarmManager.trigger(...)` 替代现有 `alarmActive = true` |
| OLED 渲染前 | 查询 `topAlarm()` 决定是否切换报警页 + `isVisualOn()` 控制闪烁 |
| 蜂鸣器控制点 | 查询 `isAudioOn()` 取代现有直接 `digitalWrite(BUZZER_PIN, ...)` |
| 按键事件 SW3 | 长按 → `operatorAck()`；栓锁报警活跃时短按 → `reset(id)` |

---

## ◆ 十六、需求符合性检查清单

| 标准 § | 条款要求摘要 | 符合 | 备注 |
|---|---|---|---|
| 4 | 报警系统作为风险控制措施 | ✅ | §三 风险评估 |
| 5.1 | 控制器/指示灯标记 | ✅ | §七-1 |
| 5.2.1 | 使用说明书内容 | ⏳ 待补 | §七-2 清单 |
| 5.2.2 | 技术说明书内容 | ⏳ 待补 | §七-3 清单 |
| 6.1.1 | 报警状态分类（生理/技术） | ✅ | §四-1 |
| 6.1.2 | 优先级分配按表 1 | ✅ | §四-2 |
| 6.2 | 智能报警系统说明 | N/A | 本设备非智能报警系统 |
| 6.3.1 | 报警状态须产生视觉信号 | ✅ | §五-(1) |
| 6.3.2.2.1 | 4m 处视觉信号 | ❌ | **偏离**：无 LED，仅 OLED（≤1m），须说明书声明 |
| 6.3.2.2.2 | 1m 处视觉信号 | ✅ | §五-(1)-1.3 |
| 6.3.3.1 | 听觉信号脉冲群特征 | ⚠️ | **降级**：固定 2.4kHz 有源蜂鸣，按脉冲数编码 |
| 6.3.3.2 | 声压级声明 | ✅ | 85dB@10cm 说明书声明 |
| 6.3.3.3 | 操作者可调音量 | N/A | 本设备不支持音量调节 |
| 6.3.4 | 语音报警信号 | N/A | 无语音合成 |
| 6.4.1 | 延迟 > 10s 需声明 | ✅ | 本设备 ≤ 6s，无须声明 |
| 6.4.2 | 分布式系统延迟 | N/A | 非分布式 |
| 6.5.1 | 报警预置通用要求 | ✅ | §八-1 |
| 6.5.2 | 制造商预置 | ✅ | 默认 38℃ |
| 6.5.3.1 | 单预置访问控制 | ✅ | 无持久化，天然满足 |
| 6.5.4.2 | 默认预置触发选择 | ✅ | 上电即默认 |
| 6.5.5 | 失电 < 30s 报警设置恢复 | ❌ | **偏离**：未启用 EEPROM，需说明书声明 |
| 6.6.1~6.6.2.3 | 报警限值 | ✅ | §九 |
| 6.7 | 报警安全性访问控制 | ✅ | §十-3（关键参数固化） |
| 6.8.1 | 提供非激活方法 | ✅ | 仅提供"声音暂停/已确认"，不提供整体关闭 |
| 6.8.2 | 提示信号 | ✅ | 每 30s 短鸣 + OLED 倒计时 |
| 6.8.3 | 整体关闭/声音关闭 | N/A | 不提供（安全决策） |
| 6.8.4 | 非激活状况终止 | ✅ | §十一-5 |
| 6.8.5 | 标记与时间间隔 | ✅ | 120s 固化 |
| 6.9 | 报警复位方法标记 | ✅ | SW3 文字提示 |
| 6.10 | 栓锁/非栓锁分配 | ✅ | §十二 |
| 6.11 | 分布式报警系统 | N/A | 独立设备 |
| 6.12 | 报警系统日志 | ⚠️ | 可选实现（§十三），否则说明书声明忽略 |

---

## ◆ 十七、偏离与限制汇总（必须在使用说明书中声明）

> 以下偏离点是本设备资源/硬件能力限制所致，须如实在使用说明书中声明：

### 1. 视觉报警信号偏离

- ❌ **未提供 4m 处可识别的颜色视觉报警信号**（标准 §6.3.2.2.1）。
- 👉 操作者必须保持 **≤1m 距离**定期查看 OLED 显示以识别报警。
- 替代编码：文本符号（`!!!`/`!!`/`!`）+ 闪烁频率 + 字号 + 反白。

### 2. 听觉报警信号偏离

- ⚠️ 蜂鸣器为**固定 2.4kHz 有源蜂鸣器**，无法满足标准 §6.3.3.1 / 表 4 中"脉冲频率 f₀ 在 150~1000Hz / 至少 4 个谐波分量在 300~4000Hz"要求。
- 👉 替代编码：仅依据**脉冲数 + 脉冲群重复周期**区分优先级（HIGH=5 脉冲/3s，MED=3 脉冲/6s，LOW=1 脉冲/不重复）。
- 声压级：≈ 85 dB(A) @ 10cm。

### 3. 音量调节不适用

- 本设备**不支持音量调节**，亦不可由操作者将音量调至 0。

### 4. 电源中断恢复偏离

- ❌ **未实现** §6.5.5 要求的"失电 < 30s 自动恢复报警设置"。
- 👉 任何失电后上电均回到默认 38℃。如需满足，后续需启用 EEPROM 持久化 + 上电校验。
- 风险评估：输液瓶加热器短时使用，断电后操作者通常会重新确认温度设定，影响可控。

### 5. 报警系统日志偏离

- ⚠️ 当前**未配备 RTC**，若实现日志仅能使用"开机后毫秒数"为时基（非实时时间）。
- 👉 可选实现循环日志（16 条），或说明书声明"本设备不具备报警日志"。

### 6. 操作者位置限制

- 标准要求操作者位置在 **1m 处**清晰识别报警 → 本设计操作者位置：**正面 0.3~1.0m**。
- 👉 说明书须强调使用环境照度 100~1500 lx、视角 ≤ ±30°。

---

## ◆ 十八、关键参数汇总表（固件实现要点）

| 参数 | 值 | 标准依据 |
|---|---|---|
| TEMP_DEFAULT | 38.0℃ | 用户需求 |
| TEMP_MIN / TEMP_MAX | 37.0℃ / 41.0℃ | 用户需求 + 痛阈保护 |
| TEMP_OVERHEAT_MED（中优超温） | 42.0℃ | 人体组织损伤阈值 |
| TEMP_OVERHEAT_MED_CLEAR | 41.5℃（迟滞） | 防抖动 |
| TEMP_OVERHEAT_HIGH（高优硬切断） | 50.0℃ | 硬件极限保护 |
| TEMP_OVERHEAT_HIGH_CLEAR | 48.0℃（迟滞） | 解锁条件 |
| ADC_SHORT_THRESHOLD / ADC_OPEN_THRESHOLD | 20 / 1000 | NTC 故障检测 |
| AUTO_SHUTDOWN_MS | 14,400,000 ms (4h) | 用户需求 |
| BUZZER_PULSE_HIGH | 125ms × 5, 间隔 100ms, 群周期 3s | 表 3 |
| BUZZER_PULSE_MED | 150ms × 3, 间隔 130ms, 群周期 6s | 表 3 |
| BUZZER_PULSE_LOW | 200ms × 1, 不重复 | 表 3 |
| MUTE_MAX_MS | 120,000 ms (120s) | 操作者静音上限 |
| REMINDER_INTERVAL_MS | 30,000 ms (30s) | 提示信号周期 |
| OLED_BLINK_HIGH_FREQ | 1Hz（500ms 亮/500ms 暗） | 表 2 |
| OLED_BLINK_MED_FREQ | 0.5Hz（1s/1s） | 折中方案 |
| POST_BUZZER_TEST_MS | 200ms | §5.2.1 |
| NTC_FILTER | 8 点移动平均，PID 周期 500ms | 抗噪 + 防抖 |

---

## ◆ 十九、后续开发路线（建议）

| 阶段 | 任务 | 优先级 |
|---|---|---|
| **A** | 创建 `alarm_manager.h/.cpp`，实现状态机骨架 + 当前最高优先级查询 | 🔴 高 |
| **B** | 实现蜂鸣器节拍状态机（50/125/150/200ms 精确时序）| 🔴 高 |
| **C** | 在 `main.cpp` 中接入 AlarmManager（替换原 `alarmActive` 布尔变量） | 🔴 高 |
| **D** | 实现 OLED 报警页 UI（不同优先级布局 + 闪烁） | 🟡 中 |
| **E** | 实现"长按 SW3 = ACK 静音"、"短按 SW3 = 栓锁复位"按键逻辑 | 🟡 中 |
| **F** | 编写使用说明书 + 技术说明书（按 §七清单） | 🟡 中 |
| **G** | 启用 POST 流程 + ALM_POST_FAIL 死循环 | 🟡 中 |
| **H** | （可选）启用 ATmega328P EEPROM 实现 §6.5.5 + 日志 | 🟢 低 |
| **I** | （可选）实现 §6.12 报警日志（循环缓冲） | 🟢 低 |
| **J** | 第三方检测机构对样机进行 YY 9706.108-2021 符合性验证 | 🟢 低（产品注册阶段） |

---

## 附：标准化术语对照表

| 中文术语 | 英文 | 标准 § |
|---|---|---|
| 报警状态 | alarm condition | 3.1 |
| 报警状态延迟 | alarm condition delay | 3.2 |
| 报警限值 | alarm limit | 3.3 |
| 报警关闭 | alarm off | 3.4 |
| 报警暂停 | alarm paused | 3.5 |
| 报警预置 | alarm preset | 3.6 |
| 报警复位 | alarm reset | 3.7 |
| 报警设置 | alarm settings | 3.8 |
| 报警信号 | alarm signal | 3.9 |
| 报警信号产生延迟 | alarm signal generation delay | 3.10 |
| 报警系统 | alarm system | 3.11 |
| 声音关闭 | audio off | 3.12 |
| 声音暂停 | audio paused | 3.13 |
| 脉冲群 | burst | 3.14 |
| 降级 | de-escalation | 3.15 |
| 默认报警预置 | default alarm preset | 3.16 |
| 分布式报警系统 | distributed alarm system | 3.17 |
| 升级 | escalation | 3.18 |
| 假阴性报警状态 | false negative alarm condition | 3.20 |
| 假阳性报警状态 | false positive alarm condition | 3.21 |
| 高优先级 | high priority | 3.22 |
| 信息信号 | information signal | 3.23 |
| 智能报警系统 | intelligent alarm system | 3.24 |
| 栓锁报警信号 | latching alarm signal | 3.26 |
| 低优先级 | low priority | 3.27 |
| 中优先级 | medium priority | 3.28 |
| 非栓锁报警信号 | non-latching alarm signal | 3.29 |
| 操作者位置 | operator's position | 3.30 |
| 生理报警状态 | physiological alarm condition | 3.31 |
| 脉冲 | pulse | 3.32 |
| 脉冲频率 | pulse frequency | 3.33 |
| 提示信号 | reminder signal | 3.34 |
| 技术报警状态 | technical alarm condition | 3.36 |
| 已确认 | acknowledged | 3.37 |

---

> 📌 **下一步建议**：先创建 `src/alarm_manager.h` 与 `src/alarm_manager.cpp` 框架（按 §十五 数据结构），实现 §五(2)-2.3 的蜂鸣器节拍状态机，然后在 `main.cpp` 中替换原有 `alarmActive` 布尔逻辑，完成报警系统接入。
